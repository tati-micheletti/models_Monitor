After knitting and rendering `models_Monitor.Rmd`, overwrite this file with a symlink to `models_Monitor.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/michelet/Documents/GitHub/models_Monitor
rm README.md && ln -s models_Monitor.md README.md
```
